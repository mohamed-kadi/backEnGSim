global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "backendlab-app"
    metrics_path: "/actuator/prometheus"
    static_configs:
      # host.docker.internal allows the Docker container to reach localhost on your host machine
      - targets: ["host.docker.internal:8080"]
